# Labov library for my thesis

## Features

- load datasets(yelp, amazonaws)
- load (sklearn-) classifiers and use them
- load (dynet-) classifiers and use them
- load (spacy textcat-) classifiers and use them
